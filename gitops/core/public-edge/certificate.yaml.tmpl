apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stardrive-wildcard
  namespace: gateway-system
spec:
  secretName: stardrive-wildcard-tls
  dnsNames:
    - "{{ .AppWildcardDomain }}"
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
